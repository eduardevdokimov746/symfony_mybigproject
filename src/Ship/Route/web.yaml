home:
    resource: ../../Container/Home/UI/WEB/Route
    type: directory

news:
    resource: ../../Container/News/UI/WEB/Route
    type: directory

auth:
    resource: ../../Container/AuthSection/Auth/UI/WEB/Route
    type: directory

reset_password:
    resource: ../../Container/AuthSection/ResetPassword/UI/WEB/Route
    type: directory

locale:
    resource: ../../Container/Locale/UI/WEB/Route
    type: directory

profile:
    resource: ../../Container/Profile/UI/WEB/Route
    type: directory

admin:
    resource: web/admin.yaml
    prefix: /admin
    name_prefix: admin.
    trailing_slash_on_root: false

when@test:
    test:
        resource: test/web.yaml
        prefix: /test
        name_prefix: test.
        trailing_slash_on_root: false

#user:
#    resource: ../../Container/User/UI/WEB/Route
#    type: directory